import 'package:flutter/material.dart';

/// App color constants for consistent branding
class AppColors {
  /// Primary brand color - Custom purple RGB(162, 91, 255)
  static const Color primary = Color(0xFFA25BFF);
  
  /// Primary color with opacity for backgrounds
  static const Color primaryLight = Color(0xFFA25BFF);
  
  /// White color for contrast against primary
  static const Color white = Colors.white;
  
  /// Black color for contrast against white backgrounds
  static const Color black = Colors.black;
} 